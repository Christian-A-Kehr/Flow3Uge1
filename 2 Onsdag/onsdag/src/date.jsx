/*
import React, {useStat} from react;

function findDate(){
    var date = new Date().getDate(); //Current Date
const [currentdate, setDate] = useStat(date);
return (
    <div>
        <p>The date is: currentdate</p>
        <button onClick={() => setDate(currentdate)} > </button>
    </div>
);
}

export default findDate; */
